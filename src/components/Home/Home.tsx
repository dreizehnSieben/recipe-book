import React, { FC } from 'react';
import styles from 'Home.module.css';

const Home: FC = () => {
    return (
        <div className={styles.container}>
            <h1>Home</h1>
        </div>
    );
};

export default Home;